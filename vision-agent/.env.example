STREAM_API_KEY=
STREAM_API_SECRET=
# Used by the live teacher's LLM (gemini.Realtime() in agent.py). Get a key at
# https://aistudio.google.com/apikey.
GOOGLE_API_KEY=
# Only used by tests/test_agent.py (LLMJudge), not by the live agent.
OPENAI_API_KEY=
# Shared secret the Expo app's /api/agent/start and /api/agent/stop routes
# send as `Authorization: Bearer <key>`. Leave unset for local dev (no auth).
AGENT_SERVICE_API_KEY=
